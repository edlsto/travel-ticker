import FilterData from './FilterData';

class User {
  constructor(id, name, type, trips) {
    this.id = id;
    this.name = name;
    this.type = type;
    this.trips = trips
  }

}

export default User;
